# Core
numpy>=1.24
opencv-python>=4.8
pyyaml>=6.0

# Perception
ultralytics>=8.0          # YOLO
pyzbar>=0.1.9             # QR decoding (optional, cv2 fallback available)

# UAV
dronekit>=2.9.2           # MAVLink / ArduPilot
pymavlink>=2.4.40

# UGV / GPS
pyserial>=3.5
pynmea2>=1.19

# GPIO — NIE instaluj lgpio przez pip!
# lgpio MUSI być zainstalowany przez apt: sudo apt install python3-lgpio
# pip install lgpio zawsze pada (brak liblgpio.so lub zły Python)
#gpiozero>=2.0

# Camera
# picamera2 MUSI być z apt: sudo apt install python3-picamera2
# lub z pip po zainstalowaniu: libcap-dev python3-kms++ python3-libcamera
#picamera2

# CLI
click>=8.0                # dualtech CLI tool

# Foxglove bridge
websockets>=12.0          # WebSocket server for Foxglove Studio

# GPIO — NIE instaluj lgpio/gpiod przez pip!
# pigpio/gpiod MUSZĄ być zainstalowane przez apt: sudo apt install pigpio python3-pigpio python3-libgpiod
# bootstrap.sh handles this automatically.

# Development / testing
pytest>=7.0
pytest-cov>=4.0