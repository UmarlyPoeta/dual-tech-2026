# Base dependencies (host-safe, Docker-compatible)
-r requirements-base.txt

# Docker-first note:
# ML dependencies (ultralytics/torch) are defined in requirements-ml.txt
# and should be installed in Docker runtime/build path.